package com.pwootage.oc.js

import java.nio.charset.StandardCharsets

import scala.math.ScalaNumber

object JSUtils {
//  def scalaToJS(invokeResult: Array[AnyRef]): Array[AnyRef] = invokeResult match {
//    case null => null
//    case _ =>
//      def conv(v: AnyRef) = v match {
//        case null => null
//        case a: Array[Byte] => new Predef.String(a, StandardCharsets.UTF_8)
//        case v: ScalaNumber => Array(v.underlying())
//        case x => x
//      }
//      invokeResult.map(conv)
//  }
//
//  def jsToJava(params: Array[AnyRef]): Array[AnyRef] = params match {
//    case null => null
//    case _ =>
//      def conv(v: AnyRef) = v match {
//        case null => null
//        case a: String => a.getBytes(StandardCharsets.UTF_8)
//        case x => x
//      }
//      params.map(conv)
//  }
}
